[project]
name = "dh-vr-v2"
version = "0.2.0"
description = "Reproducible research infrastructure for Ming-Qing Western Learning texts"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Repository-specific layered license; see docs/DATA_LICENSE_AND_RELEASE_POLICY.md"}
authors = [
  {name = "Luka / Xue Ke"}
]
keywords = [
  "digital-humanities",
  "ming-qing",
  "western-learning",
  "jesuit-studies",
  "history-of-knowledge",
  "philology",
  "reproducible-research",
  "textual-evidence"
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Topic :: Text Processing",
  "Topic :: Scientific/Engineering :: Information Analysis"
]

dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0"
]

[project.scripts]
dh-v2 = "scripts.dh_v2:main"
dh-v2-build-case = "scripts.build_case:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.dh_v2]
project_config = "config/project.yml"
metadata = "data/metadata/documents_seed.csv"
lexicon = "config/domain_lexicon_seed.csv"
raw_manifest = "data/raw/RAW_SOURCE_MANIFEST.csv"
default_segments = "data/processed/full_segments.jsonl"
